{
  "name": "map-backend-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "MAP 后端 API 联调测试",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "show-report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
